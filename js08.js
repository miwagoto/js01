function area(base, height) {
    let S = 0.5 * base * height;
    return S;
  }
console.log(area(10, 20));

  
var area = function(base, height) {
  let S = 0.5 * base * height;
  return S;
  }
console.log(area(10, 20));

  